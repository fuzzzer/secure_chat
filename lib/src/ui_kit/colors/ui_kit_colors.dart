import 'package:flutter/material.dart';

class UiKitColors {
  const UiKitColors._();

  // Light Theme Colors
  static const primaryColor = Color(0xFF007AFF); // Blue - Vibrant and fresh.
  static const focusColor = Color(0xFF5AC8FA); // Light Blue Accent.
  static const secondaryColor = Color(0xFF34C759); // Green - Complementary to Blue.
  static const errorColor = Color(0xFFFF3B30); // Red - Standard for errors.
  static const backgroundPrimaryColor = Color(0xFFFFFFFF); // White.
  static const primaryTextColor = Color(0xFF1C1C1E); // Darker shade for readability.
  static const secondaryTextColor = Color(0xFF8E8E93); // Light grey for secondary text.

  // Dark Theme Colors
  static const primaryColorDark = Color(0xFF0A84FF); // Light Blue - Soft and still vibrant.
  static const focusColorDark = Color(0xFFBF5AF2); // Soft lavender.
  static const secondaryColorDark = Color(0xFF30D158); // Bright Green.
  static const errorColorDark = Color(0xFFFF453A); // Red accent.
  static const backgroundPrimaryColorDark = Color(0xFF1C1C1E); // Dark Grey - Softer than pure black.
  static const primaryTextColorDark = Color(0xFFFFFFFF); // White.
  static const secondaryTextColorDark = Color(0xFFEBEBF5); // Softer grey for contrast against the dark background.
}
