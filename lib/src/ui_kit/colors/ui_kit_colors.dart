import 'package:flutter/material.dart';

class UiKitColors {
  const UiKitColors._();

  // Light Theme Colors
  static const primaryColor = Colors.blue;
  static const focusColor = Colors.lightBlueAccent;
  static const secondaryColor = Colors.green;
  static const errorColor = Colors.red;
  static const backgroundPrimaryColor = Colors.white;
  static const primaryTextColor = Colors.black;
  static const secondaryTextColor = Colors.grey;

  // Dark Theme Colors
  static const primaryColorDark = Colors.deepPurple;
  static const focusColorDark = Colors.purpleAccent;
  static const secondaryColorDark = Colors.teal;
  static const errorColorDark = Colors.redAccent;
  static const backgroundPrimaryColorDark = Colors.black;
  static const primaryTextColorDark = Colors.white;
  static const secondaryTextColorDark = Colors.grey;
}
